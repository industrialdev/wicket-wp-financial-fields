{
  "authors": [
    {
      "email": "info@wicket.io",
      "homepage": "https://wicket.io",
      "name": "Wicket Inc."
    }
  ],
  "autoload": {
    "classmap": [
      "src/"
    ],
    "psr-4": {
      "Wicket\\Finance\\": "src/"
    }
  },
  "config": {
    "allow-plugins": {
      "pestphp/pest-plugin": true
    },
    "platform-check": false,
    "sort-packages": true
  },
  "description": "Wicket Financial Fields - Finance mapping and deferred revenue system for WooCommerce",
  "homepage": "https://github.com/industrialdev/wicket-wp-financial-fields",
  "keywords": [
    "wordpress",
    "woocommerce",
    "finance",
    "gl-code",
    "revenue-deferral",
    "memberships"
  ],
  "license": "GPL-2.0-or-later",
  "minimum-stability": "RC",
  "name": "industrialdev/wicket-wp-financial-fields",
  "prefer-stable": true,
  "require": {
    "php": "\u003e=8.3"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3"
  },
  "scripts": {
    "check": [
      "@cs:lint"
    ],
    "cs:fix": "php-cs-fixer fix",
    "cs:format": "php-cs-fixer fix",
    "cs:lint": "php-cs-fixer fix --dry-run --diff",
    "production": "composer install && composer cs:fix && composer install --no-dev --optimize-autoloader",
    "setup-hooks": "mkdir -p .git/hooks && cp .ci/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo 'Git hooks installed to .git/hooks/pre-push'"
  },
  "scripts-descriptions": {
    "cs:format": "Run PHP CS Fixer to fix coding standards issues",
    "cs:lint": "Run PHP CS Fixer to check coding standards"
  },
  "type": "wordpress-plugin",
  "version": "1.0.8"
}
